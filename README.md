# NotchShelf

NotchShelf is currently configured for ad-hoc local signing (`CODE_SIGN_IDENTITY: "-"`).
Public distribution should switch `project.yml` to a Developer ID Application identity
and notarize the release artifact before publishing.
